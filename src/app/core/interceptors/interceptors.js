/**
 * Angular module 'phpucMonitor.core.interceptors' initialize. This will wrap up all core level interceptors that
 * application uses.
 */
(function() {
    'use strict';

    angular.module('phpucMonitor.core.interceptors', []);
}());
