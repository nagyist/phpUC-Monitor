/**
 * Angular module 'phpucMonitor.core.components' initialize. This will wrap up all core level components that
 * application uses. These 'components' contains all necessary angular logic (controllers, filters, directives, etc.)
 * for specified component to work.
 */
(function() {
    'use strict';

    angular.module('phpucMonitor.core.components', []);
}());
